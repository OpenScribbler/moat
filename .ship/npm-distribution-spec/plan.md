# Plan: npm-distribution-spec

> Spec-only feature. "Tests" in each slice are conformance assertions executed against the written spec artifacts (greps, file existence, structural assertions) and human-readable design-test reads. "Impl" is the spec/CHANGELOG/website-mirror edit that satisfies them.

## Execution order

1. **Slice 1: GitHub sub-specs reachable under `specs/github/` with platform-neutral core visible at top level** — test bead + impl bead (TDD)
   - Test: `.ship/npm-distribution-spec/conformance/slice-1.sh` — runs the five Slice-1 conformance assertions (`grep` for the old paths returns zero; `test -f` of new paths and absence of old paths; cross-reference resolution under `specs/github/...`; `lexicon.md` rows updated)
   - Impl: move `specs/publisher-action.md` → `specs/github/publisher-action.md`; move `specs/registry-action.md` → `specs/github/registry-action.md`; update `Part of:` link depth in both moved files; update cross-references in `moat-spec.md` (lines 9, 103, 143, 148, 218, 281, 286, 681, 682, 1068), `lexicon.md` (lines 43, 44), `README.md` (lines 35, 36), `RELEASING.md` (line 99), `docs/guides/publisher.md` (line 248), `reference/moat-publisher.yml` (lines 260, 482), `reference/moat-registry.yml` (line 759), `.github/workflows/moat-publisher.yml` (lines 260, 482), `.github/workflows/moat-registry.yml` (line 759)
   - Checkpoint: `find specs -name '*.md' | sort` lists exactly `specs/github/publisher-action.md`, `specs/github/registry-action.md`, `specs/moat-verify.md`; `grep -rln 'specs/publisher-action.md\|specs/registry-action.md' . | grep -v -E '^(\./)?(panel/|CHANGELOG\.md|\.ship/)'` returns no results

2. **Slice 2: npm-distribution sub-spec fixes the Tarball-Content-Directory hash domain and the materialization-boundary revocation contract** — test bead + impl bead (TDD)
   - Test: `.ship/npm-distribution-spec/conformance/slice-2.sh` — runs the four Slice-2 conformance assertions (house-style header pattern at `head -9`; every heading carries `(normative)`/`(informative)`/`(optional)` parenthetical; no algorithm re-implementation; `revoked_hashes` only referenced, not redefined) plus the three Slice-2 manual checks staged as inline assertions
   - Impl: create `specs/npm-distribution.md` with the house-style metadata header, then write `## Content Hash Domain (normative)` (citing `reference/moat_hash.py` for the algorithm, naming the unpacked Distribution Tarball's Content Directory as the input domain) and `## Revocation at the Materialization Boundary (normative)` (citing `moat-spec.md:663, 855, 865` for `revoked_hashes` semantics, anchoring MUSTs at resolve/fetch/unpack with the `MOAT_ALLOW_REVOKED` per-hash escape hatch and the resolve-time skip-logging rule)
   - Checkpoint: `specs/npm-distribution.md` exists, opens with the house-style header, contains both normative-spine sections, and a manual read against `CLAUDE.md:121-127` confirms each MUST is enforceable at the materialization boundary

3. **Slice 3: `package.json` `moat` block schema with role-discriminated `attestations[]` array and worked example readable by Publishers** — test bead + impl bead (TDD)
   - Test: `.ship/npm-distribution-spec/conformance/slice-3.sh` — runs the four Slice-3 conformance assertions (one `Field | Required | Description` table covering all required rows; fenced JSON worked example with both `role` values; canonical `_version` payload form not redefined; role-uniqueness MUST present)
   - Impl: append `## package.json moat Block (normative)` to `specs/npm-distribution.md` containing the field-definition table (`moat.contentDirectory` REQUIRED; `moat.attestations` REQUIRED array; `moat.attestations[].role` REQUIRED enum `publisher | registry`; `moat.attestations[].bundle` REQUIRED base64 Sigstore protobuf bundle v0.3; `moat.attestations[].rekor_log_index` REQUIRED integer); add the bold-label `**Role uniqueness (normative — MUST):**` qualifier; add the worked-example fenced JSON block showing a populated `moat` block with one `publisher` and one `registry` attestation entry signing the canonical `{"_version":1,"content_hash":"sha256:..."}` payload
   - Checkpoint: a Publisher copy-pastes the worked example into a real `package.json`, fills in real `content_hash` and `bundle` values, and `python -c 'import json; json.load(open("package.json"))'` parses it cleanly; a manual read of the table confirms all four `attestations[]` cardinality states (publisher-only / registry-only / both / neither) are representable

4. **Slice 4: Backfill path and npm-provenance orthogonality codified so existing npm packages can be attested without Publisher cooperation** — test bead + impl bead (TDD)
   - Test: `.ship/npm-distribution-spec/conformance/slice-4.sh` — runs the five Slice-4 conformance assertions (Backfill section heading present and normative; npm Provenance section heading present and informative; all four Trust Tier labels used consistently; no `registry_backfill_signing_profile` field introduced; closing `## Scope` section with both `**Current version:**` / `**Planned future version:**` lines)
   - Impl: append `## Backfill Attestation by Registry (normative)` (asserting the same `registry_signing_profile` is used for backfill and normal cases; clarifying that the publisher-counter-signature distinction is encoded in the Trust Tier, not in a second profile field; fixing `source_uri` for npm-only items via `moat-spec.md:766-807`); append `## npm Provenance (informative)` (observed-when-present, orthogonal to Trust Tier, MUST NOT be used to compute or override the tier); append the closing `## Scope` section
   - Checkpoint: a Registry Operator reads the Backfill section and can answer "yes, I can attest an existing npm package without the Publisher's cooperation, using my normal `registry_signing_profile`"; a Conforming Client implementer reads the npm Provenance section and can answer "no, npm provenance does not change the Trust Tier"; `specs/npm-distribution.md` is feature-complete and survives a manual pass through the four MOAT design tests

5. **Slice 5: Website mirror surfaces the new sub-spec and CHANGELOG `[Unreleased]` entry documents the change set** — test bead + impl bead (TDD)
   - Test: `.ship/npm-distribution-spec/conformance/slice-5.sh` — runs the seven Slice-5 conformance assertions (sidebar entry present in `astro.config.mjs`; mirror file exists with matching first heading; mirror parity diff shows only frontmatter / link-rewrite differences; `[Unreleased]` section exists with one `### Added` and one `### Changed` line; bold-label entries for `specs/npm-distribution.md` and `specs/github/...` files with literal `no normative change` phrase; no panel/persona/finding-ID/count language; broken-link guard on the website build)
   - Impl: create `website/src/content/docs/spec/npm-distribution.md` with Starlight YAML frontmatter and a body mirroring `specs/npm-distribution.md`; update `website/src/content/docs/spec/core.md` line 9's `**Sub-specs:**` header to add `[npm Distribution](/spec/npm-distribution)`; update `website/astro.config.mjs` Specification sidebar group (lines 87–94) to add `{ label: 'npm Distribution', slug: 'spec/npm-distribution' }`; run the audit `grep -rln 'specs/publisher-action.md\|specs/registry-action.md' website/src/` and update any path-form references found to `specs/github/...`; add the `[Unreleased]` block to `CHANGELOG.md` (one `### Added` bullet for the new sub-spec, one `### Changed` bullet for the `specs/github/` directory move with the explicit "no normative change" phrase)
   - Checkpoint: running the website's local preview command renders the Starlight sidebar with the new `/spec/npm-distribution` entry and the existing four spec entries; the new page loads with the canonical sub-spec body; `head -30 CHANGELOG.md` shows an `[Unreleased]` section whose two bullets pass the `.claude/rules/changelog.md:66-73` four-item pre-save checklist

## Gate

Before moving from one slice to the next: the Checkpoint for the current slice must pass. If it fails, stop and involve the user — never skip ahead. Slices are ordered so the directory move (Slice 1) lands before any cross-references are written into the new sub-spec, and the website mirror + CHANGELOG (Slice 5) lands last so a reader can navigate to the finished artifact and the change record describes the completed work.

## Acceptance

- `specs/npm-distribution.md` exists with: house-style metadata header (H1 / Version / Requires / Part of / blockquote / `---`), normative `package.json moat`-block field-definition table including `contentDirectory` and the role-discriminated `attestations[]` array, Tarball-Content-Directory hash-domain section citing `reference/moat_hash.py` without redefinition, materialization-boundary revocation contract citing `moat-spec.md:663, 855, 865` without redefinition, normative backfill-by-registry section, informative npm-provenance section explicit about Trust Tier orthogonality, at least one fenced JSON worked example showing a populated `moat` block with both `role: "publisher"` and `role: "registry"` entries, and a closing `## Scope` section with `**Current version:**` and `**Planned future version:**` lines.
- `specs/github/publisher-action.md` and `specs/github/registry-action.md` exist with content unchanged from their old locations modulo the `Part of:` link's relative depth; `specs/publisher-action.md` and `specs/registry-action.md` no longer exist; `specs/moat-verify.md` is unchanged.
- All cross-references to the moved files throughout `moat-spec.md`, `lexicon.md`, `README.md`, `RELEASING.md`, `docs/guides/publisher.md`, `reference/moat-publisher.yml`, `reference/moat-registry.yml`, `.github/workflows/moat-publisher.yml`, `.github/workflows/moat-registry.yml`, and the website mirror are updated to `specs/github/...`. Panel artifacts under `panel/` are intentionally untouched per `.claude/rules/changelog.md:21`.
- `CHANGELOG.md` `[Unreleased]` section contains one `### Added` bullet for the new sub-spec and one `### Changed` bullet for the directory move; both follow the bold-label form; the `### Changed` bullet carries the explicit "no normative change" phrase; neither bullet contains panel/persona/finding-ID/count language per `.claude/rules/changelog.md:23-32`.
- The new sub-spec specifies what is hashed for npm-distributed content (the unpacked Distribution Tarball's Content Directory) and explicitly explains the relationship to npm's tarball SHA-512 and npm provenance — npm provenance is observed-when-present and orthogonal to Trust Tier; npm's tarball SHA-512 is npm's own integrity primitive, not the MOAT Content Hash.
- The new sub-spec defines a backfill path so a Registry can attest a pre-existing npm package without Publisher cooperation, using the same `registry_signing_profile` as a normal Registry attestation.
- The new sub-spec passes the four MOAT design tests in `CLAUDE.md:121-127` (day-one, copy-survival, works-fine-without-it, enforcement); each MUST has a Conforming Client enforcement mechanism (refuse to materialize at the materialization boundary).
- The website mirror at `website/src/content/docs/spec/npm-distribution.md` exists and is reachable via the Starlight sidebar; existing sub-spec slugs `spec/publisher-action` and `spec/registry-action` are preserved.
- House-style conformance: every section heading in the new sub-spec carries an RFC-2119-status parenthetical (`(normative)`, `(normative — MUST)`, `(normative — SHOULD)`, `(informative)`, or `(optional)`); inline normative qualifiers use the bold-label form per the design's Pattern: Bold-label inline normative qualifiers.

## Non-TDD exemptions

None. Every slice has machine-checkable conformance assertions; the test/impl pairing is preserved even though the artifacts are Markdown rather than code.

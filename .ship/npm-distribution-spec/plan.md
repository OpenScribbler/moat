# Plan: npm-distribution-spec — Round 2

> Spec-only feature (Round 2 deltas on top of Round 1 commit `64b9c6b`). "Tests" in each slice are conformance assertions executed against the written spec artifacts (greps, file existence, structural assertions, fixture-based hash equalities) and human-readable design-test reads. "Impl" is the spec/CHANGELOG/website-mirror/reference-workflow edit that satisfies them. Round 1 closed 10 beads under prefix `moat` (`moat-27q`/`moat-8uf` … `moat-53v`/`moat-41g`); Round 2 starts a fresh 7-pair test+impl chain plus a final-validate bead.

## Execution order

1. **Slice 1: Default-Content-Directory backfill capability** — test bead + impl bead (TDD)
   - Test: `.ship/npm-distribution-spec/conformance/slice-6.sh` — runs the Slice 1 conformance assertions: two independent reductions of fixture tarball `t1` (files `a.md`, `b.js`, `package.json`) produce byte-equal canonical hashes when `moat.tarballContentRoot` is absent; mutating `package.json` between reductions does not change the hash; subdirectory mode (`moat.tarballContentRoot: "src"`) applies no exclusions and includes `src/package.json` in the domain; default-mode exclusion is path-anchored to tarball root so a nested `pkg/package.json` is INCLUDED and only root-level `package.json` is EXCLUDED; backfill path (no Publisher cooperation) and Publisher-driven path produce byte-equal hashes against `t1`
   - Impl: append `## Content Directory (normative — MUST)` to `specs/npm-distribution.md` defining the default = unpacked tarball root with `package.json` excluded, the subdirectory-mode no-exclusion rule, and the MUST-NOT against Publisher-driven extension of the exclusion list; bump the file-level metadata header from v0.1.0 Draft to v0.2.0 Draft; add a `### Changed` bold-label bullet to `CHANGELOG.md` `[Unreleased]` for the default-Content-Directory rule (no panel/finding-ID language per `.claude/rules/changelog.md:23-32`); sync the byte-identical body to `website/src/content/docs/spec/npm-distribution.md` (after Starlight front-matter)
   - Checkpoint: `bash .ship/npm-distribution-spec/conformance/slice-6.sh` exits 0; `grep -c '## Content Directory' specs/npm-distribution.md` returns ≥ 1; `diff <(sed '1,/^---$/d' website/src/content/docs/spec/npm-distribution.md) specs/npm-distribution.md` exits 0; `grep -F 'v0.2.0' specs/npm-distribution.md` matches; the Round 1 conformance scripts (`slice-1.sh`..`slice-5.sh`) still exit 0

2. **Slice 2: `tarballContentRoot` field-name realization with lexicon cross-reference** — test bead + impl bead (TDD)
   - Test: inline conformance assertions executed against the artifacts (no new `slice-*.sh` since this slice introduces no new domain logic): `grep -c 'moat\.contentDirectory' specs/npm-distribution.md` returns 0; `grep -c 'moat\.contentDirectory' website/src/content/docs/spec/npm-distribution.md` returns 0; the line containing `` `tarballContentRoot` `` inside the Content Directory entry of `lexicon.md` exists and references `package.json`; the Description column for `moat.tarballContentRoot` in `specs/npm-distribution.md` cross-references the lexicon's Content Directory entry rather than re-defining the concept; mirror byte-identity diff exits 0
   - Impl: rename the `moat.contentDirectory` row in the field table of `specs/npm-distribution.md § package.json moat block (normative)` to `moat.tarballContentRoot` and replace every `moat.contentDirectory` literal in JSON examples and prose with `moat.tarballContentRoot` (Draft-status breaking change permitted by `moat-spec.md:14-16` and `:22`, no historical note per design.md Question 3 recommended option); add a one-line realization note to the Content Directory entry in `lexicon.md` per the Pattern: Field-name realizes lexicon concept; add a `### Changed` bold-label bullet to `CHANGELOG.md` `[Unreleased]` for the field rename (stating that no migration scaffolding is provided because the spec is Draft with zero adopters per `moat-spec.md:22`); sync the website mirror
   - Checkpoint: `grep -F 'moat.contentDirectory' specs/npm-distribution.md website/src/content/docs/spec/npm-distribution.md` returns no matches; `grep -F 'tarballContentRoot' lexicon.md` returns ≥ 1 match inside the Content Directory entry; `diff <(sed '1,/^---$/d' website/src/content/docs/spec/npm-distribution.md) specs/npm-distribution.md` exits 0

3. **Slice 3: Process-scoped revocation override with auditable expiry** — test bead + impl bead (TDD)
   - Test: `.ship/npm-distribution-spec/conformance/slice-7.sh` — runs the Slice 3 conformance assertions: the override section names all four MUSTs (process-scope read-once, REQUIRED `MOAT_ALLOW_REVOKED_REASON` co-variable with hard-fail enforcement, `<sha256-hex>:<RFC3339-timestamp>` per-entry encoding, structured override-applied event); the spec cites the design.md Question 1 recommended log-event field names `package`, `content_hash`, `reason`, `expires_at`; the spec states that entries past their RFC 3339 timestamp are treated as if absent (no warning, no log); the spec states that override entries without the timestamp delimiter MUST be ignored as malformed (no permanent overrides); mirror byte-identity diff exits 0
   - Impl: rewrite `specs/npm-distribution.md § MOAT_ALLOW_REVOKED Operator Override (normative)` to the expanded form, replacing the Round 1 minimal section: enumerate the four normative MUSTs with explicit anchors per the Pattern: Process-scope environment variable read once at start, the Pattern: Co-variable required for risky operation, and the Pattern: RFC 3339 timestamps for protocol-time fields; specify the structured override-applied event with field names `package`, `content_hash`, `reason`, `expires_at`; add a `### Changed` bold-label bullet to `CHANGELOG.md` `[Unreleased]` for the override hardening (stating the new prerequisites and structured-event field names, no finding-ID/persona language); sync the website mirror
   - Checkpoint: `bash .ship/npm-distribution-spec/conformance/slice-7.sh` exits 0; `grep -cE 'MOAT_ALLOW_REVOKED_REASON' specs/npm-distribution.md` returns ≥ 2 (definition + hard-fail rule); `grep -cE 'expires_at' specs/npm-distribution.md` returns ≥ 1; the Round 1 conformance scripts still exit 0

4. **Slice 4: Cache-boundary materialization anchor** — test bead + impl bead (TDD)
   - Test: inline conformance assertions: `specs/npm-distribution.md` contains the literal phrase `before any byte of the tarball is written outside the package manager's content cache`; the section names `resolve`, `fetch`, and `unpack` as the operations a Conforming Client may refuse at; the section states that whichever sub-operation the Client refuses at, no extracted bytes may land outside the cache; the Round 1 prose at `specs/npm-distribution.md:29` is superseded (`grep` for any Round-1-specific phrasing the rephrase replaces returns 0); mirror byte-identity diff exits 0
   - Impl: replace the Round 1 prose at the existing materialization-boundary location in `specs/npm-distribution.md` (line 29 region) with the cache-boundary anchor verbatim and an informative-tagged paragraph naming `pacote`, Yarn Plug'n'Play, and the pnpm content-addressable store as the architectures the rule maps cleanly onto (per design decision B-1); add a `### Changed` bold-label bullet to `CHANGELOG.md` `[Unreleased]` describing the rephrase as a normative clarification (not a behavior change); sync the website mirror
   - Checkpoint: `grep -F "before any byte of the tarball is written outside the package manager's content cache" specs/npm-distribution.md` returns ≥ 1 match; `grep -cE 'resolve.*fetch.*unpack|fetch.*unpack' specs/npm-distribution.md` returns ≥ 1; the Round 1 conformance scripts still exit 0

5. **Slice 5: Publisher identity disclosure via `publisherSigning` block** — test bead + impl bead (TDD)
   - Test: inline conformance assertions: the field-table for `publisherSigning.issuer` and `publisherSigning.subject` marks both REQUIRED; `publisherSigning.rekorLogIndex` is OPTIONAL; the Round 1 `attestations[].bundle` Publisher-role row does NOT appear; the Registry-role `attestations[]` row remains; the sub-spec body cites `moat-spec.md`'s `signing_profile` (anchored link or section reference) per the Pattern: Sigstore Rekor authoritative + identity disclosure in metadata; the Conforming-Client verification MUSTs are present for both `rekorLogIndex` present and absent cases; mirror byte-identity diff exits 0
   - Impl: rewrite the Publisher-role section of the `moat` block schema in `specs/npm-distribution.md § package.json moat block (normative)` — relocate Publisher signing identity out of `attestations[].bundle` into a top-level `publisherSigning` block with REQUIRED `issuer`, REQUIRED `subject`, OPTIONAL `rekorLogIndex`; preserve the `attestations[]` array for the Registry role only; add a new `## Publisher Verification (normative)` subsection enumerating the two verification paths (when `rekorLogIndex` is present, fetch by index and validate identity; when absent, query Rekor by canonical Content Hash and filter by `{issuer, subject}`); add a `### Changed` bold-label bullet to `CHANGELOG.md` `[Unreleased]` stating the schema-shape change is a Draft-status breaking change with no migration scaffolding; sync the website mirror
   - Checkpoint: `grep -cE 'publisherSigning\.(issuer|subject|rekorLogIndex)' specs/npm-distribution.md` returns ≥ 3; `grep -c 'attestations\[\].bundle' specs/npm-distribution.md` returns 0; `grep -F 'signing_profile' specs/npm-distribution.md` returns ≥ 1 match; the Round 1 conformance scripts still exit 0

6. **Slice 6: Four-state provenance × MOAT disagreement table** — test bead + impl bead (TDD)
   - Test: inline conformance assertions: the `## npm Provenance` section in `specs/npm-distribution.md` contains a Markdown pipe table with at least four data rows; the rows cover all four states (both present, MOAT-only, provenance-only, neither); each row names the Conforming-Client display rule and the Trust Tier impact; the section states that npm provenance and Trust Tier are orthogonal axes; mirror byte-identity diff exits 0
   - Impl: append a four-row pipe table to the existing `## npm Provenance (informative)` section of `specs/npm-distribution.md` (the section currently lives around line 107-115 per design decision B-2); columns are `(npm provenance, MOAT attestation, Conforming Client display, Trust Tier impact)`; rows enumerate (both present, MOAT-only, provenance-only, neither) per the recommended Option A in design.md Question 2; add the explicit orthogonality statement; add a `### Changed` bold-label bullet to `CHANGELOG.md` `[Unreleased]` for the new informative table; sync the website mirror
   - Checkpoint: `awk '/## npm Provenance/,/^## /' specs/npm-distribution.md | grep -c '^|'` returns ≥ 5 (header row + separator + four data rows); `grep -F 'orthogonal' specs/npm-distribution.md` returns ≥ 1 match; the Round 1 conformance scripts still exit 0

7. **Slice 7: End-to-end npm Publisher reference workflow + cross-spec rider edits** — test bead + impl bead (TDD)
   - Test: `.ship/npm-distribution-spec/conformance/slice-8.sh` — runs the Slice 7 conformance assertions: `reference/moat-npm-publisher.yml` declares exactly seven named steps in order (`npm pack` v1, compute canonical hash, Sigstore sign, push to Rekor, write log index back to `package.json`, `npm pack` v2, `npm publish`); the `permissions:` block contains `id-token: write` and `contents: write`; the workflow `uses: sigstore/cosign-installer@...`; the `on:` block covers release tag push and `workflow_dispatch`; `moat-spec.md:9` contains the literal `specs/npm-distribution.md`; `.claude/rules/changelog.md:40` cites `specs/github/publisher-action.md` and does not cite the pre-reorg path; the two-pack canonical-hash stability invariant holds against a fixture (steps 1-6 produce tarball v1 and tarball v2; the canonical hash computed by Slice 1's rule is byte-equal between the two tarballs because `package.json` is excluded under default mode); mirror byte-identity diff exits 0
   - Impl: create `reference/moat-npm-publisher.yml` mirroring `reference/moat-publisher.yml:1-80`'s structure exactly (same `permissions` block, same `sigstore/cosign-installer` step shape, same job/step layout) per the Pattern: Reference workflow as adoption-mechanism template; the seven steps realize the canonical "compute hash, sign payload, log to Rekor" flow over the npm channel with the two-pack discipline making the canonical hash stable across the log-index round-trip; append a `## Reference Implementations` bullet to `specs/npm-distribution.md` citing `reference/moat-npm-publisher.yml` (mirroring `moat-spec.md:681`'s pattern of citing `reference/moat-publisher.yml` from `specs/github/publisher-action.md`); edit `moat-spec.md:9` to add `specs/npm-distribution.md` to the comma-separated Sub-specs list (decision A-1); edit `.claude/rules/changelog.md:40` from the pre-reorg `specs/publisher-action.md` to the post-reorg `specs/github/publisher-action.md` (decision A-2; per `.claude/rules/changelog.md:21`'s tooling-only exclusion no CHANGELOG entry is required for this file); add a `### Added` bold-label bullet to `CHANGELOG.md` `[Unreleased]` for the new reference workflow; sync the website mirror
   - Checkpoint: `bash .ship/npm-distribution-spec/conformance/slice-8.sh` exits 0; `test -f reference/moat-npm-publisher.yml` succeeds; `grep -F 'specs/npm-distribution.md' moat-spec.md | head -1` matches at line 9; `grep -F 'specs/github/publisher-action.md' .claude/rules/changelog.md` matches at line 40 and `grep -F 'specs/publisher-action.md' .claude/rules/changelog.md` returns no match (the pre-reorg path is gone); the Round 1 conformance scripts (`slice-1.sh`..`slice-5.sh`) still exit 0; the new conformance scripts (`slice-6.sh`..`slice-8.sh`) all exit 0

## Gate

Before moving from one slice to the next: the Checkpoint for the current slice must pass. If it fails, stop and involve the user — never skip ahead. Slices are ordered so the load-bearing default-Content-Directory rule (Slice 1) and its dependent field rename (Slice 2) land before any of the smaller normative refinements (Slices 3-6); the reference workflow + cross-spec rider edits (Slice 7) land last because the workflow's two-pack invariant depends on Slice 1's exclusion rule and the rider edits are the discoverability surface that should reflect a finished sub-spec. Within each slice, the test bead's conformance script is written before the impl bead is started; the impl bead is closed only after the conformance assertions exit 0 and the Round 1 scripts still exit 0 (no normative-surface regression).

## Acceptance

- `specs/npm-distribution.md` is at v0.2.0 Draft and contains: a default Content Directory rule (tarball root with `package.json` exclusion); the renamed `moat.tarballContentRoot` field with a lexicon cross-reference; the expanded `MOAT_ALLOW_REVOKED` section with reason co-variable, RFC 3339 expiry, and structured override-applied event; the cache-boundary materialization anchor verbatim; the `publisherSigning` block with REQUIRED `issuer`/`subject` and OPTIONAL `rekorLogIndex` plus Conforming-Client verification MUSTs; the four-state provenance × MOAT disagreement table with orthogonality statement; and a Reference Implementations bullet citing `reference/moat-npm-publisher.yml`. (Slices 1-7.)
- `lexicon.md`'s Content Directory entry carries a one-line note that `tarballContentRoot` (in `package.json`) is one realization of the concept. (Slice 2.)
- `reference/moat-npm-publisher.yml` exists, mirrors `reference/moat-publisher.yml:1-80`'s structure, declares the seven steps in order, and produces canonical-hash-stable two-pack behavior under fixture conditions. (Slice 7.)
- `moat-spec.md` line 9's Sub-specs line cites `specs/npm-distribution.md`. (Slice 7.)
- `.claude/rules/changelog.md` line 40 cites `specs/github/publisher-action.md` and does not cite the pre-reorg path. (Slice 7.)
- `CHANGELOG.md` `[Unreleased]` carries `### Changed` bullets for the default-Content-Directory rule, the field rename, the `MOAT_ALLOW_REVOKED` hardening, the materialization-boundary rephrase, the `publisherSigning` schema change, and the four-state disagreement table; and an `### Added` bullet for the new reference workflow. Each bullet is in the bold-label form per `.claude/rules/changelog.md:48`, contains no panel/persona/finding-ID language per `.claude/rules/changelog.md:23-32`, and stands alone for a reader with zero internal context per `.claude/rules/changelog.md:46`. (Slices 1-7.)
- `website/src/content/docs/spec/npm-distribution.md` is byte-identical to `specs/npm-distribution.md` after stripping Starlight front-matter, on every commit that touches the canonical sub-spec. (Slices 1-7.)
- The Round 1 conformance scripts `.ship/npm-distribution-spec/conformance/slice-1.sh` through `slice-5.sh` continue to exit 0 on a fresh checkout (no Round 1 normative-surface regression).
- New conformance scripts `slice-6.sh` (default-Content-Directory hashing equality), `slice-7.sh` (override-event log shape and four-MUSTs presence), and `slice-8.sh` (two-pack canonical-hash stability) exit 0 on a fresh checkout.
- The four MOAT design tests (`CLAUDE.md:121-127`) hold for the override section: day-one (operator can override on the day the spec ships), copy-survival (override semantics survive being copied to another Conforming Client implementation because the structured event is the audit anchor), works-fine-without-it (the hard-fail on missing reason closes the silent-skip path), enforcement (the structured event provides the detection mechanism — there is no "trust that operators will write good logs").
- ADRs 0005-0009 (auto-drafted from the Round 2 design's Disambiguation blocks) flip from Proposed to Accepted via the ship-adr-handler hook on the slice commits that land their respective decisions; this is hook-managed and not a plan-level acceptance criterion.

## Non-TDD exemptions

None. Every slice has machine-checkable conformance assertions; the test/impl pairing is preserved even though the artifacts are Markdown, YAML, or JSON-schema rather than executable code. Slices 2, 4, 5, and 6 use inline conformance assertions on the artifact body (greps/structural checks) in lieu of a new `slice-*.sh` script because they introduce no new domain logic beyond the assertion itself; Slices 1, 3, and 7 carry new conformance scripts (`slice-6.sh`, `slice-7.sh`, `slice-8.sh`) that require fixture setup or multi-step invariants.
